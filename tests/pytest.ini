
[pytest]
filterwarnings =
    ignore::DeprecationWarning
junit_family=xunit1
log_cli = False
log_cli_level = INFO
log_cli_format = %(asctime)s.%(msecs)03dZ|%(levelname)s|%(threadName)s|%(funcName)s|%(filename)s#%(lineno)d||%(message)s
log_cli_date_format = %Y-%m-%dT%H:%M:%S
log_file = pytest-logs.txt
log_file_level = DEBUG
addopts =
          --forked
          --json-report
          --json-report-file=build/reports/report.json
          --cov-branch
          --cov-report term
          --cov-report html:build/reports/htmlcov
          --cov-report xml:build/code-coverage.xml
          --cov=ska_tmc_centralnode
          --junitxml=build/reports/unit-tests.xml
          --cucumberjson=build/cucumber.json
          --verbose
console_output_style = count
markers =
    post_deployment: run only with a real deployment of the ska tmc centralnode repository
    SKA_mid: run on SKA mid only
    SKA_low: run on SKA low only
    acceptance: run with real deployment and outside the context of the ska tmc centralnode repository
    long_running: run long running tets only
    XTP-3612
    XTP-3613
    XTP-3614
    XTP-3615
bdd_features_base_dir = tests/features


[coverage:run]
data_file = .coverage/.coverage
branch = True
source = src

[coverage:paths]
source =
    src

[coverage:report]
show_missing = True
